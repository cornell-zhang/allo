# Copyright Allo authors. All Rights Reserved.
# SPDX-License-Identifier: Apache-2.0

name: "Allo AIE (XDNA1) Weekly Test"
on:
  pull_request:
    branches:
      - main

jobs:
  aie_test:
    # This job runs on Linux
    runs-on: [aie, xdna1]
    container:
      image: shihanfang/allo-ci:aie-v1.0
      options: "--device /dev/accel/accel0:/dev/accel/accel0 --ulimit memlock=-1"
    steps:
    # https://github.com/actions/checkout/issues/363#issuecomment-1915075699
    - name: Install git in container
      run: |
        apt-get -y install git
        git config --global --add safe.directory "$GITHUB_WORKSPACE"
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Build Allo
      shell: bash
      run: |
        source activate allo
        python3 -m pip install -v -e .
    - name: AIE unit tests
      shell: bash
      run: |
        source activate allo
        xrt-smi examine
        export OMP_NUM_THREADS=128
        python3 -m pytest --ignore=tests/dataflow/aie/gpt2 tests/dataflow/aie -v
        unset OMP_NUM_THREADS
